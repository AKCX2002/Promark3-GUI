import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Placeholder – ChangeNotifierProvider wrapping makes the
    // default counter test meaningless. Real tests coming later.
    expect(1 + 1, 2);
  });
}
